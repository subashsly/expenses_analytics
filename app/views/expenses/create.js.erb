$("ul.errors").html("")
<% if @expense.errors.any? %>
	<% @expense.errors.full_messages.each do |msg| %>
		$("ul.errors").append($("<li />").html("<%= msg.html_safe %>"))
	<% end %>
<% else %>
	$("#transaction").append("<%= j render @expense%>")
	$("#expense-modal").modal("hide")
<% end %>