$("ul.errors").html("")
<% if @income.errors.any? %>
	<% @income.errors.full_messages.each do |msg| %>
		$("ul.errors").append($("<li />").html("<%= msg.html_safe %>"))
	<% end %>
<% else %>
var total =$("h3.total_expense").text();
	 
	 		if (total != "") {
	 			  location.reload();
	 		}
	$("#transaction").append("<%= j render @income%>")
	$("#income-modal").modal("hide")
<% end %>