<% provide(:title, "Edit Member") %>
<div class="container">


<div class="row">
	<div class="col-md-6">
	<h1 class="form-title">Update your profile</h1> 
		<%= form_for(@member) do |f| %>
			<%= render 'shared/error_messages' %>

			<%= f.label :name %>
			<%= f.text_field :member_name, class: 'form-control' %>

			<%= f.label :email %>
			<%= f.text_field :member_email, class: 'form-control' %>

			<%= f.label :password %>
			<%= f.password_field :password, class: 'form-control' %>

			<%= f.label :password_confirmation, "Confirmation" %>
			<%= f.password_field :password_confirmation, class: 'form-control' %>

			<%= f.label :country_code, "Country" %>
			<%= f.country_select :member_country, prompt: "Select a Country" %>

			<%= f.submit "Update Changes", class: 'btn sign-up' %>
		<% end %>

		<div class="gravatar_edit">
			<%= gravatar_for @member %>
			<a href="http://gravatar.com/emails">Change</a>
		</div>
	</div>
</div>
</div>