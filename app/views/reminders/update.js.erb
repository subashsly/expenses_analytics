$("ul.errors").html("")
<% if @reminder.errors.any? %>
	<% @reminder.errors.full_messages.each do |msg| %>
		$("ul.errors").append($("<li />").html("<%= msg.html_safe %>"))
	<% end %>
<% else %>
	$('#reminder-<%= @reminder.id %>').replaceWith('<%= j render(@reminder) %>') 
	$("#reminder-modal").modal("hide")
<% end %>
